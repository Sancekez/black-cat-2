include ../count-block/template

mixin iconInline(data)
	.icon-inline&attributes(attributes)
		._icon
			+svg(data.icon)
		._wrap
			!= data.text

mixin iconInlineSearch(data)
	.icon-inline.-default&attributes(attributes)
		a._link.js-search-mob-show(href="#")
		._icon
			+svg(data.icon)

mixin iconInlineDefault(data) 
	.icon-inline.-default&attributes(attributes)
		a._link(href="#")
		._icon
			._counter 2
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					!= data.text

mixin iconInlineLogin(data)
	.icon-inline.-default.-login&attributes(attributes)
		a._link.js-popup-show(href="#popup-login")
		._icon
			._counter 2
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					!= data.text

mixin iconInlineCubeDashed(data)
	.icon-inline.-cube-dashed&attributes(attributes)
		a._link(href="#")
		._icon
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					!= data.text

mixin iconInlineCubeShadow(data)
	.icon-inline.-cube-shadow&attributes(attributes)
		._icon
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					!= data.text
		._bottom
			!= data.bottom

mixin iconInlineCubeShadow2(data)
	.icon-inline.-cube-shadow&attributes(attributes)
		._title.-top
			!= data.title
		._icon
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._text
					!= data.text
		

mixin iconInlineCart(data)
	.icon-inline.-default.-cart.js-cart-toggle&attributes(attributes)
		//- a._link(href="#")
		._icon
			._counter 2
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					!= data.text
		._hidden
			._body-costs
				._item
					._item-title Сосенка водяная<br> (хвостник обыкновенный)
					._item-count
						+countBlockSmall()
					._item-price
						._cost 2 270 ₽
					._item-del
						a._btn(href="#")
							+svg('delete-2')
				._item
					._item-title Калла Манго
					._item-count
						+countBlockSmall()
					._item-price
						._cost 2 230 ₽
					._item-del
						a._btn(href="#")
							+svg('delete-2')
				._item
					._item-title Вахта трехлистная<br> (трилистник водяной)
					._item-count
						+countBlockSmall()
					._item-price
						._cost-none 170 ₽
					._item-del
			._total
				._total-text Итого:
				._total-cost <strong>4 600.00 ₽</strong>
			._buttons
				a.btn.-sm.-yellow(href="#") Купить в 1 клик
				a.btn.-sm.-green(href="#") Оформить заказ

//- mixin iconInlineLogin(data)
	.icon-inline&attributes(attributes)
		._icon
			+svg('user')
		._wrap.-login
			a.js-popup-show(href="#popup-login") Вход
			span |
			a.js-popup-show(href="#popup-registration-1") Регистрация

mixin iconInlineLogout(data)
	.icon-inline.-default.-logout&attributes(attributes)
		._icon
			+svg(data.icon)
		if data.title || data.text
			._wrap
				._title
					!= data.title
				._text
					a(href="#") 
						!= data.text
		._hidden
			._menu 
				._menu-item 
					a(href="#") Заказы
				._menu-item 
					a(href="#") Адреса доставки
				._menu-item 
					a(href="#") Бонусы и скидки
				._menu-item 
					a(href="#") Личные данные
				._menu-item 
					a(href="#") Уведомления
				._menu-item 
					a(href="#") Подписки
				._menu-item 
					a(href="#") 
						span Выйти из профиля
						+svg('exit')

mixin iconInlinePhone(data)
	.icon-inline&attributes(attributes)
		._title 
			!= data.title
		._row
			._icon
				+svg(data.icon)
			._wrap
				._phone 
					!= data.phone
				p 
					!= data.subtext